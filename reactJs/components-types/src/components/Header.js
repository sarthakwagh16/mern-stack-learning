import React from "react";

// the name of the file and the component should be same
//first letter of component name should be capital

function Header(){
    return <h1>I am a functional component</h1>
}
export default Header;

export const Greet=()=>{
    return(
        <h1>Good evening react</h1>
    )
}